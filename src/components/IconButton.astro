---
import IconCustom from "./IconCustom.astro";

export interface Props {
	srOnly: string;
  type: string;
  svgName: string;
  svgNameHover: string;
}

// import svgReference from '../../assets/home-pressed.svg';

const {svgName, svgNameHover, type, srOnly} = Astro.props as Props;

---

<button type={type} class="text-black border-2 border-transparent bg-light-secondary shadow-button-flat-nopressed hover:border-2 hover:shadow-button-flat-pressed focus:opacity-100 focus:outline-none active:border-2 active:shadow-button-flat-pressed font-medium rounded-full text-sm p-2.5 text-center inline-flex items-center mr-4 last-of-type:mr-0">
  <IconCustom
    svgName={ svgName }
    svgNameHover={ svgNameHover }
    svgWidth= '25px'
    svgHeight= '25px'
  />
  <span class="sr-only"> {srOnly} </span>
</button>
